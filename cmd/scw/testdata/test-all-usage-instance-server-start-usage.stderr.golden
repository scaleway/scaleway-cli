Power on server

USAGE:
  scw instance server start <server-id> [arg=value ...]

EXAMPLES:
  Start a server in the default zone with a given id
    scw instance server start server-id=11111111-1111-1111-1111-111111111111

  Start a server in fr-par-1 zone with a given id
    scw instance server start zone=fr-par-1 server-id=11111111-1111-1111-1111-111111111111

ARGS:
  server-id   ID of the server affected by the action.
  [zone]      Zone to target. If none is passed will use default zone from the config

FLAGS:
  -h, --help   help for start
  -w, --wait   wait until the server is ready

GLOBAL FLAGS:
  -D, --debug            Enable debug mode
  -o, --output string    Output format: json or human
  -p, --profile string   The config profile to use
