🎲🎲🎲 EXIT CODE: 0 🎲🎲🎲
🟥🟥🟥 STDERR️️ 🟥🟥🟥️
Network Access Control Lists allow you to control incoming network traffic by setting up ACL rules.

USAGE:
  scw rdb acl <command>

AVAILABLE COMMANDS:
  add          Add an ACL rule to a Database Instance
  add-multi    Add multiple ACL rules in one command
  delete       Delete ACL rules of a Database Instance
  delete-multi Delete multiple ACL rules in one command
  edit         Edit a database instance's ACL
  list         List ACL rules of a Database Instance
  set          Set ACL rules for a Database Instance

FLAGS:
  -h, --help   help for acl

GLOBAL FLAGS:
  -c, --config string    The path to the config file
  -D, --debug            Enable debug mode
  -o, --output string    Output format: json or human, see 'scw help output' for more info (default "human")
  -p, --profile string   The config profile to use

Use "scw rdb acl [command] --help" for more information about a command.
